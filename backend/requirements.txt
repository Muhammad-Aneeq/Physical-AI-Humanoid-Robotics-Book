# FastAPI and ASGI server
fastapi[all]==0.109.0
uvicorn[standard]==0.27.0

# OpenAI Agents SDK (for agent-based LLM responses)
openai-agents==0.2.9

# Sentence Transformers for local embeddings (BAAI/bge-base-en-v1.5, 768 dims)
sentence-transformers>=2.7.0,<4.0.0
torch>=2.5.0,<3.0.0

# Qdrant vector database client
qdrant-client==1.7.0

# SQLAlchemy ORM and PostgreSQL driver
sqlalchemy==2.0.25
psycopg[binary]==3.1.16
alembic==1.13.1

# Pydantic for validation (updated for openai-agents compatibility)
pydantic>=2.10,<3.0
pydantic-settings>=2.5.2

# Rate limiting
slowapi==0.1.9

# Environment variables
python-dotenv==1.0.0

# Markdown parsing and text splitting
markdown-it-py==3.0.0
langchain-text-splitters==0.0.1

# YAML parsing for frontmatter
pyyaml==6.0.1

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx>=0.27.1  # For async testing (updated for mcp compatibility)

# Error tracking (optional)
sentry-sdk[fastapi]==1.40.0
